# xiao-tang-AI-Companion

# TODO
- [ ] fix the display duration of the overlay, it should disappear after it speaks, makes it dynamic duration ,disappear if and ONLY IF when the tts is over.
- [ ] make the video full screen
<<<<<<< Updated upstream
- [ ] implement agentic workflow, add plan in the memory doc for orchestration layer, each session executes a step of the plan and keep updating the mega doc so the next session will remember the progress and don't have to restart.
- [ ] CONVEY Project - similar to conovey, let AI be autonomous, assign it a task, and let it complete the task by itself while the streamer is gone. The AI has no limit in taking screenshot and taking actions. It can take over control of the keyboard and the mouse. It can set up a repo by itself, it can run any command in the terminal other than removing stuff. we can set up some basic guardrails. But the idea is it can watch youtube watch whatever. If it needs some access to stuff it can write it down as proposal to get access to certain stuff in a folder so the user can come back and populate and satisfy the requests so that they can proceed in next session. 
- [ ] Make Language switch a togglable thing in .env
- [ ] Actually make a GUI for the settings, so that the user can change the settings without going into the .env file. 
- [ ] make it able to read youtube chat
- [ ] upgrade AI voice system. 

=======
>>>>>>> Stashed changes
- [ ] CONVEY Project - similar to conovey, let AI be autonomous, assign it a task, and let it complete the task by itself while the streamer is gone. The AI has no limit in taking screenshot and taking actions. It can take over control of the keyboard and the mouse. It can set up a repo by itself, it can run any command in the terminal other than removing stuff. we can set up some basic guardrails. But the idea is it can watch youtube watch whatever. If it needs some access to stuff it can write it down as proposal to get access to certain stuff in a folder so the user can come back and populate and satisfy the requests so that they can proceed in next session. 
- [ ] Make Language switch a togglable thing in .env
- [ ] Actually make a GUI for the settings, so that the user can change the settings without going into the .env file. 
- [ ] make it able to read youtube chat
- [ ] upgrade AI voice system. 